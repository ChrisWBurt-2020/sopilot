specVersion: "v1"
serviceId: "sopilot"
location: "us-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "sopilot-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
